from sets import Set
from cheating_set import CheatingSet
from utils import *

def powerset_bf(s):
	'''
	Return all subsets of `s` using a brute force approach
	'''
	pass
	
def powerset_lo(s):
	'''
	Return all subsets of `s` using a lexical order approach
	'''
	pass
	
def powerset_md(s):
	'''
	Return all subsets of `s` using a minimal difference order approach
	'''
	pass
	
def rank_lo(t, n) -> int:
	'''
	Return the rank of a subset `t` from a set S = {1, ..., n}
	The rank is calculated according a lexical order approach
	'''
	pass
	
def unrank_lo(r, n) -> int:
	'''
	Return the the subset `t` from a set S = {1, ..., n} that has a rank `n`
	The rank is calculated according a lexical order approach
	'''
	pass
	
def successor_lo(t, n):
	'''
	Given a subset `t` from a set S = {1, ..., n}
	Return the next subset according to a lexical order approach
	'''
	pass
	
def rank_md(t, n) -> int:
	'''
	Return the rank of a subset `t` from a set S = {1, ..., n}
	The rank is calculated according a minimal difference order approach
	'''
	pass
	
def unrank_md(r, n) -> int:
	'''
	Return the the subset `t` from a set S = {1, ..., n} that has a rank `n`
	The rank is calculated according a minimal difference order approach
	'''
	pass
	
def successor_md(t, n):
	'''
	Given a subset `t` from a set S = {1, ..., n}
	Return the next subset according to a minimal difference order approach
	'''
	pass

